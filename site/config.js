/**
 * config.js — a ÚNICA coisa a preencher para ligar o site ao backend.
 * A URL do Web App não é segredo (o backend valida tudo do seu lado).
 * Preencha depois do deploy (ver docs/SETUP.md).
 */
window.CONFIG = {
  BASE_URL: 'COLE_AQUI_A_URL_DO_WEB_APP'
};
